# Build-time environment variables for Cloudflare Pages
# These are dummy values used only during the build phase
# Real values should be configured in Cloudflare Pages dashboard

# Database (not used during build, but needed to prevent errors)
DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy

# Auth
BETTER_AUTH_SECRET=dummy-secret-key-for-build-only-min-32-chars-long
NEXT_PUBLIC_APP_URL=https://example.com

# Dodo Payments (dummy for build)
DODO_PAYMENTS_API_KEY=dummy_key_for_build
DODO_PAYMENTS_WEBHOOK_SECRET=dummy_webhook_secret
NEXT_PUBLIC_STARTER_TIER=dummy_product_id

# Email (dummy for build)
RESEND_API_KEY=re_dummy_key_for_build
RESEND_FROM_EMAIL=noreply@example.com

# Google OAuth (dummy for build)
GOOGLE_CLIENT_ID=dummy_client_id
GOOGLE_CLIENT_SECRET=dummy_client_secret

# LiveKit (dummy for build)
LIVEKIT_API_KEY=dummy_livekit_key
LIVEKIT_API_SECRET=dummy_livekit_secret
LIVEKIT_URL=wss://example.livekit.cloud

# Zoho (dummy for build)
ZOHO_CLIENT_ID=dummy_zoho_client
ZOHO_CLIENT_SECRET=dummy_zoho_secret
ZOHO_REDIRECT_URI=https://example.com/api/zoho/auth/callback

# App
NEXT_PUBLIC_APP_NAME=InvoCall
